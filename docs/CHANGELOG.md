# Changelog

Все важные изменения в этом проекте будут документированы в этом файле.

## [1.2.2] - 2026-01-05

### Улучшено
- 🔍 Добавлено debug-логирование для диагностики WebSocket подключения
  - Логируется полный ответ от API `/api/devices/wss/`
  - Попытка извлечения URL из разных возможных ключей (url, wss_url, websocket_url)
  - Расширенное логирование для выявления проблемы с WebSocket

### Примечание
Для просмотра debug-логов добавьте в configuration.yaml:
```yaml
logger:
  logs:
    custom_components.ujin: debug
```

## [1.2.1] - 2026-01-05

### Исправлено
- 🐛 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Добавлен недостающий импорт `API_DEVICES_WSS`
  - Исправлена ошибка "name 'API_DEVICES_WSS' is not defined" в api.py
  - WebSocket подключение теперь работает корректно

## [1.2.0] - 2026-01-05

### Добавлено
- 🔥 **WebSocket для real-time обновлений!**
  - Добавлен WebSocket клиент для мгновенных обновлений состояния устройств
  - Состояние обновляется в реальном времени при изменении через любое приложение
  - Автоматическое переподключение при разрыве соединения
  - Fallback на polling если WebSocket недоступен
  - **UI обновляется мгновенно без задержек и "прыжков"**

- 🎨 Исправлено отображение иконок устройств
  - Заменен property `icon` на `_attr_icon` с инициализацией
  - Расширена логика определения иконок по модели устройства

### Улучшено
- Упрощен код switch.py - убраны костыли с `_last_command_time`
- Оптимистичное обновление UI + WebSocket для подтверждения
- Coordinator работает как fallback для WebSocket

### Техническое описание
- Создан `websocket.py` с классом `UjinWebSocketClient`
- Добавлен метод `get_websocket_url()` в `api.py`
- При setup интеграции подключается к `/api/devices/wss/`
- WebSocket триггерит `coordinator.async_request_refresh()` при получении сообщений
- В `async_turn_on/off`:
  1. Оптимистично обновляется `_attr_is_on`
  2. Вызывается `async_write_ha_state()` для мгновенного UI
  3. WebSocket получает обновление от сервера
  4. Coordinator обновляет данные
- При unload интеграции WebSocket корректно отключается

## [1.1.4] - 2026-01-05

### Исправлено
- 🐛 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Устранена race condition при обновлении состояния
  - Убран немедленный вызов `async_request_refresh()` после команды
  - Добавлена защита от перезаписи оптимистичного состояния устаревшими данными
  - Coordinator игнорирует обновления в течение 5 секунд после отправки команды
  - UI больше не "прыгает", состояние обновляется корректно
  - История активности отображается мгновенно
  - Повторные команды работают без сбоев

- 🎨 Исправлено отображение иконок устройств
  - Заменен property `icon` на `_attr_icon` с инициализацией
  - Иконки теперь отображаются правильно для всех типов устройств
  - Добавлено определение иконки по модели устройства
  - Иконка синхронизируется при обновлении данных от coordinator

### Улучшено
- Расширена логика определения иконок:
  - `mdi:water-pump` для Ujin Aqua (контроллер протечки)
  - `mdi:electric-switch` для Ujin Connect-din (DIN-реле)
  - `mdi:lightbulb-multiple` для Ujin Connect-dim (диммер)
  - `mdi:lightbulb` для освещения
  - `mdi:power-socket` для розеток
  - `mdi:toggle-switch` для остальных устройств

### Техническое описание
- Добавлено отслеживание времени последней команды (`_last_command_time`)
- В `async_turn_on/off`:
  1. Устанавливается `_last_command_time = datetime.now()`
  2. Оптимистично обновляется `_attr_is_on`
  3. Вызывается `async_write_ha_state()` для мгновенного UI
  4. НЕ вызывается `async_request_refresh()` - избегаем race condition
- В `_handle_coordinator_update()`:
  - Проверяется время с последней команды
  - Если прошло < 5 секунд - обновление игнорируется
  - Если прошло ≥ 5 секунд - состояние синхронизируется с coordinator.data
  - Это дает устройству время физически выполнить команду
- Создан метод `_get_icon_for_device()` для определения иконки
- Иконка инициализируется в `__init__` как `_attr_icon`
- Удален property `icon` в пользу `_attr_icon`

## [1.1.3] - 2026-01-05

### Исправлено
- 🐛 **ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ**: Правильный паттерн optimistic updates
  - Использован правильный паттерн с `_attr_is_on` вместо property `is_on`
  - UI обновляется мгновенно без задержек и "прыжков"
  - История активности отображается сразу при нажатии
  - Состояние синхронизируется с реальным через coordinator

### Техническое описание
- **Удален** property `is_on` - теперь используется встроенный механизм `_attr_is_on`
- В `async_turn_on/off`:
  1. Оптимистично обновляется `self._attr_is_on = True/False`
  2. Вызывается `self.async_write_ha_state()` для мгновенного обновления UI
  3. Запрашивается `await self.coordinator.async_request_refresh()` для синхронизации
- В `_handle_coordinator_update()`:
  - Читается реальное состояние из `coordinator.data`
  - Обновляется `_attr_is_on` с реальным значением
  - Вызывается `async_write_ha_state()` для применения
- Реализован правильный паттерн согласно официальной документации Home Assistant

### Важно
Версии 1.1.1-1.1.2 использовали неправильный подход - откачены в 1.1.3

## [1.1.2] - 2026-01-05

### Исправлено (НЕПРАВИЛЬНЫЙ ПОДХОД - ОТКАЧЕНО В 1.1.3)
- 🐛 **ПРАВИЛЬНОЕ ИСПРАВЛЕНИЕ**: Мгновенное обновление истории активности
  - Теперь используется правильный подход с `async_write_ha_state()`
  - Локальное состояние обновляется немедленно после успешной команды
  - История активности отображает действие сразу, без задержек
  - Убрана искусственная задержка в 2 секунды

### Техническое описание
- При нажатии на переключатель команда отправляется на устройство
- После успешной команды обновляется `self._device_data["controls"][0]["value"]`
- Вызывается `self.async_write_ha_state()` для немедленной записи в state machine
- Это обновляет историю активности и UI мгновенно
- Coordinator продолжает обновлять данные каждые 30 секунд для синхронизации
- Исправлено согласно официальной документации Home Assistant

### Важно
Версия 1.1.1 использовала неправильный подход с задержкой - она откачена в 1.1.2

## [1.1.1] - 2026-01-05

### Исправлено (НЕПРАВИЛЬНЫЙ ПОДХОД - ОТКАЧЕНО В 1.1.2)
- 🐛 Исправлено мерцание UI переключателей
  - Добавлена задержка 2 секунды перед обновлением состояния
  - Устранена race condition между отправкой команды и обновлением API
  - Теперь переключатель плавно меняет состояние без "прыжков"

### Техническое описание
- При нажатии на переключатель команда отправляется на устройство
- Добавлена задержка `await asyncio.sleep(2)` перед `async_request_refresh()`
- Это позволяет API Ujin обновить состояние устройства на сервере
- Только после этого Home Assistant запрашивает актуальное состояние
- Исправлено в методах `async_turn_on()` и `async_turn_off()`

## [1.1.0] - 2026-01-05

### Добавлено
- 📖 Подготовлены файлы для brands repository
  - Папка brands_submission/ с правильно подготовленными иконками
  - Инструкция по добавлению иконки в официальный brands repository
  - README с пошаговыми инструкциями

### Документация
- Добавлен раздел "Иконка интеграции" в README
- Объяснение почему иконка может не отображаться
- Инструкция по созданию PR в home-assistant/brands

## [1.0.9] - 2026-01-05

### Добавлено
- ✅ Добавлена полная поддержка HACS
  - Создан hacs.json конфигурационный файл
  - Логотипы добавлены в корень репозитория
  - Обновлен .gitignore

### Примечание об иконках
- Иконка интеграции может не отображаться в Home Assistant UI
- Это известное ограничение для кастомных интеграций
- Требуется регистрация в официальном brands репозитории Home Assistant
- **Это не влияет на функциональность интеграции** - все устройства работают корректно

## [1.0.8] - 2026-01-05

### Улучшено
- ✨ Использование официального логотипа Ujin
  - Заменена простая буква "U" на настоящий логотип с ujin.tech
  - Более профессиональный внешний вид в списке интеграций
  - Обновлены logo.png, logo@2x.png и icon.png

### Примечание
- После обновления перезагрузите Home Assistant для применения нового логотипа

## [1.0.7] - 2026-01-05

### Исправлено
- 🔧 Исправлено отображение иконки интеграции в Home Assistant
  - Добавлен файл logo.png (требуется для кастомных интеграций)
  - Теперь иконка корректно отображается в списке интеграций

### Примечание
- После обновления необходимо перезагрузить Home Assistant для применения иконки

## [1.0.6] - 2026-01-05

### Добавлено
- ✨ Добавлены иконки для интеграции
  - icon.png (256x256) - иконка интеграции в Home Assistant
  - logo@2x.png (512x512) - высокое разрешение для HACS
  - Синий фон с белой буквой "U" (Ujin)

### Улучшено
- Иконки устройств уже присутствовали и корректно отображаются
  - mdi:lightbulb для освещения
  - mdi:power-socket для розеток
  - mdi:water-pump для контроллера протечки

## [1.0.5] - 2026-01-05

### Исправлено
- 🔧 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Использование правильного токена для запросов
  - Добавлена поддержка user_token (токен квартиры) отдельно от основного токена
  - Теперь используется user_token из квартиры для запросов к devices/main
  - Исправлена проблема "Нет прикрепленных устройств" даже с правильным area_guid
  - user_token сохраняется и восстанавливается между перезагрузками

### Технические детали
- Добавлено поле `_user_token` в UjinApiClient
- Метод `_get_apartments()` теперь извлекает user_token из ответа
- Методы `get_devices()` и `send_device_command()` используют user_token
- ConfigEntry теперь сохраняет оба токена: token и user_token

## [1.0.4] - 2026-01-05

### Исправлено
- 🔧 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Правильное извлечение area_guid
  - Добавлен endpoint `/api/v4/mobile/profile/objects/select/` для получения списка квартир
  - area_guid теперь извлекается в формате UUID (как в мобильном приложении)
  - Исправлена проблема с пустым списком устройств
  - Удалена неправильная логика использования apartment_id как area_guid

### Технические детали
- Добавлен метод `_get_apartments()` в UjinApiClient
- area_guid теперь получается после авторизации из списка квартир пользователя
- Формат area_guid: UUID (например, `2758d30b-c7db-48ae-9948-add515e37ace`)
- Поддержка нескольких квартир (используется первая)

## [1.0.3] - 2026-01-05

### Исправлено
- Попытка извлечения area_guid из apartment_id (не решило проблему)

## [1.0.2] - 2026-01-05

### Добавлено
- Расширенное debug-логирование для диагностики проблем

## [1.0.1] - 2026-01-05

### Исправлено
- Исправлена версия в manifest.json (было 0.1.0, стало 1.0.0)
- Исправлены URL репозитория в manifest.json

## [1.0.0] - 2026-01-05

### Добавлено
- ✅ Полная интеграция с Ujin API
- ✅ Email-аутентификация с кодом подтверждения
- ✅ Платформа switch для всех устройств
- ✅ Поддержка Ujin Connect-din (dinrelay_m4)
- ✅ Поддержка Ujin Connect-dim (ujin-zdm-m2)
- ✅ Поддержка Ujin Aqua (ujin-zld-m1)
- ✅ Автоматическое обновление состояния каждые 30 секунд
- ✅ Хранение токена между перезагрузками
- ✅ Обработка истечения токена
- ✅ Полная документация API
- ✅ Примеры автоматизаций

### Технические детали
- Email-аутентификация с двухфакторным кодом
- Автоматическое извлечение area_guid из API
- Корректный парсинг структуры устройств
- Правильная отправка команд управления
- Все устройства отображаются как switch entities

## Планы на будущее

### [1.1.0]
- [ ] WebSocket для real-time обновлений
- [ ] Уменьшение интервала опроса

### [1.2.0]
- [ ] Binary sensor для датчиков
- [ ] Sensor для показаний (температура, влажность)
- [ ] Локальное управление через sapfir-unicast

### [2.0.0]
- [ ] HACS integration
- [ ] Документация на английском
- [ ] Поддержка всех типов устройств Ujin
